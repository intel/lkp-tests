suite: kselftests-bpf
testcase: kselftests-bpf
category: functional

# NOET: need_memory might be different for different rootfs
# Currently, these value came from the meminfo.max_used_kB during runtime
# on debian rootfs
need_memory: 12G

need_cpu: 2

kselftests-bpf:
  group:
    - bpf
    - drivers
    - drivers/net
    - drivers/net/bonding
    - drivers/net/hw
    - drivers/net/netdevsim
    - hid
    - sched_ext
