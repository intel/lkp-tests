#!/bin/sh

. $LKP_SRC/lib/reproduce-log.sh

program=$1
program_dir=$(dirname "$0")

runner_path=$program_dir/$program
[ -x "$runner_path" ] || runner_path=$LKP_SRC/programs/$program/setup

shift

[ -x "$runner_path" ] && exec "$runner_path" "$@"
