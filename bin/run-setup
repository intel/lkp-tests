#!/bin/sh

. $LKP_SRC/lib/reproduce-log.sh

program=$1
program_dir=$(dirname "$0")

program_path=$program_dir/$program
[ -x "$program_path" ] || program_path=$LKP_SRC/programs/$program/setup

shift

[ -x "$program_path" ] && exec "$program_path" "$@"
